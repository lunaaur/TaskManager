import { clrBlue } from "@variables";
import styled from "styled-components/native";

export const ButtonTouchable = styled.TouchableOpacity`
`

export const ButtonText = styled.Text`
color: ${clrBlue};
text-decoration: ${clrBlue} underline;
`